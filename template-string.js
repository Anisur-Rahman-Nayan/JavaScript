const firstName = 'Anisur'
const middledName = "Rahman"
const lastName = `Nayan`
const multiLine = `${firstName} ${middledName} ${lastName}`

const count = 5
const newHeader = `<h3 class="friend> Friend-${count} </h3>"`